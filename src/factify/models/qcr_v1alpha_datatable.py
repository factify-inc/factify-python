"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_columndef import QcrV1alphaColumnDef, QcrV1alphaColumnDefTypedDict
from .qcr_v1alpha_datarow import QcrV1alphaDataRow, QcrV1alphaDataRowTypedDict
from .value import Value, ValueTypedDict
from factify.types import BaseModel, Nullable, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class QcrV1alphaDataTableTypedDict(TypedDict):
    r"""DataTable is the semantic core: schema + records.
    For CSV: one DataTable per file. For XLSX: one per ListObject or detected
    table region. When a sheet is a single table, there is one DataTable.
    """

    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    columns: NotRequired[List[QcrV1alphaColumnDefTypedDict]]
    r"""the schema"""
    has_total_row: NotRequired[bool]
    name: NotRequired[str]
    r"""from ListObject or extractor-assigned"""
    range_ref: NotRequired[str]
    r"""A1-notation origin, e.g. \"A1:D100\" """
    rows: NotRequired[List[QcrV1alphaDataRowTypedDict]]
    r"""ordered data rows (excludes header)"""


class QcrV1alphaDataTable(BaseModel):
    r"""DataTable is the semantic core: schema + records.
    For CSV: one DataTable per file. For XLSX: one per ListObject or detected
    table region. When a sheet is a single table, there is one DataTable.
    """

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    columns: Optional[List[QcrV1alphaColumnDef]] = None
    r"""the schema"""

    has_total_row: Optional[bool] = None

    name: Optional[str] = None
    r"""from ListObject or extractor-assigned"""

    range_ref: Optional[str] = None
    r"""A1-notation origin, e.g. \"A1:D100\" """

    rows: Optional[List[QcrV1alphaDataRow]] = None
    r"""ordered data rows (excludes header)"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["attributes", "columns", "has_total_row", "name", "range_ref", "rows"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
