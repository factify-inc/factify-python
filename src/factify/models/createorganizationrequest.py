"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from factify.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class CreateOrganizationRequestTypedDict(TypedDict):
    r"""CreateOrganizationRequest contains the data needed to create a new organization."""

    name: str
    r"""Human-readable organization name."""
    owner_user_id: NotRequired[Nullable[str]]
    r"""Owner user ID. If not provided, the authenticated user becomes the owner.
    Authorization: Specifying a different user requires system maintainer role.
    Required when the caller is a service account (service accounts cannot own organizations).
    Pattern: user_[0-9a-hjkmnp-tv-z]{26}
    """


class CreateOrganizationRequest(BaseModel):
    r"""CreateOrganizationRequest contains the data needed to create a new organization."""

    name: str
    r"""Human-readable organization name."""

    owner_user_id: OptionalNullable[str] = UNSET
    r"""Owner user ID. If not provided, the authenticated user becomes the owner.
    Authorization: Specifying a different user requires system maintainer role.
    Required when the caller is a service account (service accounts cannot own organizations).
    Pattern: user_[0-9a-hjkmnp-tv-z]{26}
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["owner_user_id"])
        nullable_fields = set(["owner_user_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
