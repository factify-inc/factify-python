"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_codeblockdata import (
    QcrV1alphaCodeBlockData,
    QcrV1alphaCodeBlockDataTypedDict,
)
from .qcr_v1alpha_footnotedata import (
    QcrV1alphaFootnoteData,
    QcrV1alphaFootnoteDataTypedDict,
)
from .qcr_v1alpha_headingdata import (
    QcrV1alphaHeadingData,
    QcrV1alphaHeadingDataTypedDict,
)
from .qcr_v1alpha_listdata import QcrV1alphaListData, QcrV1alphaListDataTypedDict
from .qcr_v1alpha_paragraphdata import (
    QcrV1alphaParagraphData,
    QcrV1alphaParagraphDataTypedDict,
)
from .qcr_v1alpha_quotedata import QcrV1alphaQuoteData, QcrV1alphaQuoteDataTypedDict
from .qcr_v1alpha_rasterimagedata import (
    QcrV1alphaRasterImageData,
    QcrV1alphaRasterImageDataTypedDict,
)
from .qcr_v1alpha_tabledata import QcrV1alphaTableData, QcrV1alphaTableDataTypedDict
from .qcr_v1alpha_thematicbreakdata import (
    QcrV1alphaThematicBreakData,
    QcrV1alphaThematicBreakDataTypedDict,
)
from .qcr_v1alpha_vectorimagedata import (
    QcrV1alphaVectorImageData,
    QcrV1alphaVectorImageDataTypedDict,
)
from .value import Value, ValueTypedDict
from factify.types import BaseModel, Nullable, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional, TYPE_CHECKING, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict

if TYPE_CHECKING:
    from .qcr_v1alpha_sectiondata import (
        QcrV1alphaSectionData,
        QcrV1alphaSectionDataTypedDict,
    )


class ThematicBreakTypedDict(TypedDict):
    thematic_break: QcrV1alphaThematicBreakDataTypedDict
    r"""Explicit authorial boundary (e.g. markdown `---`). Leaf block, no children."""
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class ThematicBreak(BaseModel):
    thematic_break: QcrV1alphaThematicBreakData
    r"""Explicit authorial boundary (e.g. markdown `---`). Leaf block, no children."""

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class QcrV1alphaBlockTableTypedDict(TypedDict):
    table: QcrV1alphaTableDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class QcrV1alphaBlockTable(BaseModel):
    table: QcrV1alphaTableData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CodeBlockTypedDict(TypedDict):
    code_block: QcrV1alphaCodeBlockDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class CodeBlock(BaseModel):
    code_block: QcrV1alphaCodeBlockData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FigureTypedDict(TypedDict):
    figure: QcrV1alphaVectorImageDataTypedDict
    r"""VectorImageData represents a vector graphic (SVG, diagram, chart)."""
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Figure(BaseModel):
    figure: QcrV1alphaVectorImageData
    r"""VectorImageData represents a vector graphic (SVG, diagram, chart)."""

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class FootnoteTypedDict(TypedDict):
    footnote: QcrV1alphaFootnoteDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Footnote(BaseModel):
    footnote: QcrV1alphaFootnoteData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class HeadingTypedDict(TypedDict):
    heading: QcrV1alphaHeadingDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Heading(BaseModel):
    heading: QcrV1alphaHeadingData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ImageTypedDict(TypedDict):
    image: QcrV1alphaRasterImageDataTypedDict
    r"""RasterImageData represents a raster image (PNG, JPEG, WebP, etc.)."""
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Image(BaseModel):
    image: QcrV1alphaRasterImageData
    r"""RasterImageData represents a raster image (PNG, JPEG, WebP, etc.)."""

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ListTTypedDict(TypedDict):
    list: QcrV1alphaListDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class ListT(BaseModel):
    list: QcrV1alphaListData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ParagraphTypedDict(TypedDict):
    paragraph: QcrV1alphaParagraphDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Paragraph(BaseModel):
    paragraph: QcrV1alphaParagraphData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class QuoteTypedDict(TypedDict):
    quote: QcrV1alphaQuoteDataTypedDict
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Quote(BaseModel):
    quote: QcrV1alphaQuoteData

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SectionTypedDict(TypedDict):
    section: "QcrV1alphaSectionDataTypedDict"
    r"""Section groups blocks under a logical heading. Only sections can nest blocks."""
    attributes: NotRequired[Dict[str, Nullable[ValueTypedDict]]]
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """
    id: NotRequired[str]
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """


class Section(BaseModel):
    section: "QcrV1alphaSectionData"
    r"""Section groups blocks under a logical heading. Only sections can nest blocks."""

    attributes: Optional[Dict[str, Nullable[Value]]] = None
    r"""`Struct` represents a structured data value, consisting of fields
    which map to dynamically typed values. In some languages, `Struct`
    might be supported by a native representation. For example, in
    scripting languages like JS a struct is represented as an
    object. The details of that representation are described together
    with the proto support for the language.

    The JSON representation for `Struct` is JSON object.
    """

    id: Optional[str] = None
    r"""Optional stable ID for external references (claim anchoring, annotations).
    Not used for layout addressing — positional paths serve that purpose.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["attributes", "id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


QcrV1alphaBlockTypedDict = TypeAliasType(
    "QcrV1alphaBlockTypedDict",
    Union[
        CodeBlockTypedDict,
        FigureTypedDict,
        FootnoteTypedDict,
        HeadingTypedDict,
        ImageTypedDict,
        ListTTypedDict,
        ParagraphTypedDict,
        QuoteTypedDict,
        "SectionTypedDict",
        QcrV1alphaBlockTableTypedDict,
        ThematicBreakTypedDict,
    ],
)
r"""Block is the unit of document content. Each block carries exactly one
type-specific payload via oneof. Blocks do not nest generically — only
SectionData can contain child blocks, enforcing valid containment at the
schema level.
"""


QcrV1alphaBlock = TypeAliasType(
    "QcrV1alphaBlock",
    Union[
        CodeBlock,
        Figure,
        Footnote,
        Heading,
        Image,
        ListT,
        Paragraph,
        Quote,
        "Section",
        QcrV1alphaBlockTable,
        ThematicBreak,
    ],
)
r"""Block is the unit of document content. Each block carries exactly one
type-specific payload via oneof. Blocks do not nest generically — only
SectionData can contain child blocks, enforcing valid containment at the
schema level.
"""
