"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .documentbundle import DocumentBundle, DocumentBundleTypedDict
from .spreadsheetbundle import SpreadsheetBundle, SpreadsheetBundleTypedDict
from factify.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict


class SpreadsheetTypedDict(TypedDict):
    spreadsheet: SpreadsheetBundleTypedDict
    r"""SpreadsheetBundle groups a spreadsheet record with its optional grid layout."""
    id: NotRequired[str]
    r"""Unique record ID (UUID format)."""
    source_format: NotRequired[str]
    r"""Source format of the original upload (e.g., pdf, docx, xlsx, csv)."""


class Spreadsheet(BaseModel):
    spreadsheet: SpreadsheetBundle
    r"""SpreadsheetBundle groups a spreadsheet record with its optional grid layout."""

    id: Optional[str] = None
    r"""Unique record ID (UUID format)."""

    source_format: Optional[str] = None
    r"""Source format of the original upload (e.g., pdf, docx, xlsx, csv)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "source_format"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class GetRecordResponseDocumentTypedDict(TypedDict):
    document: DocumentBundleTypedDict
    r"""DocumentBundle groups a document record with its optional spatial layout."""
    id: NotRequired[str]
    r"""Unique record ID (UUID format)."""
    source_format: NotRequired[str]
    r"""Source format of the original upload (e.g., pdf, docx, xlsx, csv)."""


class GetRecordResponseDocument(BaseModel):
    document: DocumentBundle
    r"""DocumentBundle groups a document record with its optional spatial layout."""

    id: Optional[str] = None
    r"""Unique record ID (UUID format)."""

    source_format: Optional[str] = None
    r"""Source format of the original upload (e.g., pdf, docx, xlsx, csv)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "source_format"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


GetRecordResponseTypedDict = TypeAliasType(
    "GetRecordResponseTypedDict",
    Union[GetRecordResponseDocumentTypedDict, SpreadsheetTypedDict],
)
r"""GetRecordResponse contains the processed content and layout for a version.
The content oneof is populated based on source_format:
- document formats (pdf, docx, markdown): document field
- spreadsheet formats (xlsx, csv):        spreadsheet field
"""


GetRecordResponse = TypeAliasType(
    "GetRecordResponse", Union[GetRecordResponseDocument, Spreadsheet]
)
r"""GetRecordResponse contains the processed content and layout for a version.
The content oneof is populated based on source_format:
- document formats (pdf, docx, markdown): document field
- spreadsheet formats (xlsx, csv):        spreadsheet field
"""
