"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from factify.types import BaseModel, UNSET_SENTINEL
from factify.utils import FieldMetadata, QueryParamMetadata
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ListAPIKeysRequestTypedDict(TypedDict):
    organization_id: str
    r"""Organization ID to list keys for.
    Pattern: org_[0-9a-hjkmnp-tv-z]{26}
    """
    include_revoked: NotRequired[bool]
    r"""Whether to include revoked keys in the response.
    Default: false (only active and expired-but-not-revoked keys are returned).
    When true, all keys including revoked ones are returned.
    Note: Expired keys are always included unless also revoked; use is_active to filter.
    """
    page_token: NotRequired[str]
    r"""Opaque pagination token from a previous response."""
    page_size: NotRequired[int]
    r"""Maximum number of items to return per page (1-100). Default: 50."""


class ListAPIKeysRequest(BaseModel):
    organization_id: Annotated[
        str, FieldMetadata(query=QueryParamMetadata(style="form", explode=True))
    ]
    r"""Organization ID to list keys for.
    Pattern: org_[0-9a-hjkmnp-tv-z]{26}
    """

    include_revoked: Annotated[
        Optional[bool],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Whether to include revoked keys in the response.
    Default: false (only active and expired-but-not-revoked keys are returned).
    When true, all keys including revoked ones are returned.
    Note: Expired keys are always included unless also revoked; use is_active to filter.
    """

    page_token: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Opaque pagination token from a previous response."""

    page_size: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Maximum number of items to return per page (1-100). Default: 50."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["include_revoked", "page_token", "page_size"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
