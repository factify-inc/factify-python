"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_mimetype import QcrV1alphaMimeType
from factify.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict


class StorageRefTypedDict(TypedDict):
    storage_ref: str
    r"""S3 key, CDN URL, or other stable reference"""
    mime_type: NotRequired[QcrV1alphaMimeType]


class StorageRef(BaseModel):
    storage_ref: str
    r"""S3 key, CDN URL, or other stable reference"""

    mime_type: Optional[QcrV1alphaMimeType] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["mime_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class InlineDataTypedDict(TypedDict):
    inline_data: str
    r"""raw bytes for small/embedded content"""
    mime_type: NotRequired[QcrV1alphaMimeType]


class InlineData(BaseModel):
    inline_data: str
    r"""raw bytes for small/embedded content"""

    mime_type: Optional[QcrV1alphaMimeType] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["mime_type"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


QcrV1alphaDataSourceTypedDict = TypeAliasType(
    "QcrV1alphaDataSourceTypedDict", Union[InlineDataTypedDict, StorageRefTypedDict]
)
r"""DataSource represents a media payload — either a stable external reference
or inline bytes — paired with its MIME type.
"""


QcrV1alphaDataSource = TypeAliasType(
    "QcrV1alphaDataSource", Union[InlineData, StorageRef]
)
r"""DataSource represents a media payload — either a stable external reference
or inline bytes — paired with its MIME type.
"""
