"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_linespacing import QcrV1alphaLineSpacing
from factify.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class QcrV1alphaLineSpacingValueTypedDict(TypedDict):
    r"""Line spacing as mode + optional explicit value.
    Using a message instead of a bare enum avoids a future wire break when
    exact/at-least modes are added (they need a companion float value).
    """

    mode: NotRequired[QcrV1alphaLineSpacing]
    value_mm: NotRequired[float]
    r"""meaningful only for EXACT / AT_LEAST (future)"""


class QcrV1alphaLineSpacingValue(BaseModel):
    r"""Line spacing as mode + optional explicit value.
    Using a message instead of a bare enum avoids a future wire break when
    exact/at-least modes are added (they need a companion float value).
    """

    mode: Optional[QcrV1alphaLineSpacing] = None

    value_mm: Optional[float] = None
    r"""meaningful only for EXACT / AT_LEAST (future)"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["mode", "value_mm"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
