"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_boundingbox import (
    QcrV1alphaBoundingBox,
    QcrV1alphaBoundingBoxTypedDict,
)
from factify.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class QcrV1alphaLayoutFragmentTypedDict(TypedDict):
    r"""A single spatial fragment of an element on a specific page."""

    bounding_box: NotRequired[QcrV1alphaBoundingBoxTypedDict]
    r"""Normalized bounding box relative to the page (0.0–1.0).
    Origin is top-left: (0,0) is the top-left corner of the page,
    x increases rightward, y increases downward.
    Extraction pipelines must flip the y-axis for bottom-left origin formats (PDF).
    To get absolute coordinates, multiply by the page's PhysicalDimensions.
    """
    page_number: NotRequired[int]
    r"""0-indexed, references PageGeometry.page_number"""
    rotation_degrees: NotRequired[float]


class QcrV1alphaLayoutFragment(BaseModel):
    r"""A single spatial fragment of an element on a specific page."""

    bounding_box: Optional[QcrV1alphaBoundingBox] = None
    r"""Normalized bounding box relative to the page (0.0–1.0).
    Origin is top-left: (0,0) is the top-left corner of the page,
    x increases rightward, y increases downward.
    Extraction pipelines must flip the y-axis for bottom-left origin formats (PDF).
    To get absolute coordinates, multiply by the page's PhysicalDimensions.
    """

    page_number: Optional[int] = None
    r"""0-indexed, references PageGeometry.page_number"""

    rotation_degrees: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["bounding_box", "page_number", "rotation_degrees"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
