"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .qcr_v1alpha_cellerrortype import QcrV1alphaCellErrorType
from factify.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict


class StringValueTypedDict(TypedDict):
    string_value: str
    hyperlink: NotRequired[str]
    note: NotRequired[str]


class StringValue(BaseModel):
    string_value: str

    hyperlink: Optional[str] = None

    note: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["hyperlink", "note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class NumberValueTypedDict(TypedDict):
    number_value: float
    hyperlink: NotRequired[str]
    note: NotRequired[str]


class NumberValue(BaseModel):
    number_value: float

    hyperlink: Optional[str] = None

    note: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["hyperlink", "note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class ErrorValueTypedDict(TypedDict):
    error_value: QcrV1alphaCellErrorType
    hyperlink: NotRequired[str]
    note: NotRequired[str]


class ErrorValue(BaseModel):
    error_value: QcrV1alphaCellErrorType

    hyperlink: Optional[str] = None

    note: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["hyperlink", "note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class BoolValueTypedDict(TypedDict):
    bool_value: bool
    hyperlink: NotRequired[str]
    note: NotRequired[str]


class BoolValue(BaseModel):
    bool_value: bool

    hyperlink: Optional[str] = None

    note: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["hyperlink", "note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


QcrV1alphaDataCellTypedDict = TypeAliasType(
    "QcrV1alphaDataCellTypedDict",
    Union[
        BoolValueTypedDict,
        ErrorValueTypedDict,
        NumberValueTypedDict,
        StringValueTypedDict,
    ],
)
r"""DataCell holds a single semantic value. Source-format concerns (formulas,
formatting) are not modeled — the record captures what the data means.
"""


QcrV1alphaDataCell = TypeAliasType(
    "QcrV1alphaDataCell", Union[BoolValue, ErrorValue, NumberValue, StringValue]
)
r"""DataCell holds a single semantic value. Source-format concerns (formulas,
formatting) are not modeled — the record captures what the data means.
"""
